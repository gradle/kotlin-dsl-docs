dokka {
  moduleName = "api"
  outputDirectory = "build/docs/dokka"
  jdkVersion = 8
  processConfigurations = [ ]
  sourceDirs = [ new File( cloneGSK.outputDirectory.singleFile.absoluteFile, 'src/main/kotlin' ), copyGradleApiSources.outputDirectory.singleFile ]
               //new File( cloneGradle.outputDirectory, "subprojects" ).listFiles().
               //  collect { new File( it.absoluteFile, 'src/main/java' ) }
}
