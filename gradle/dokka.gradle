dokka {
  moduleName = "api"
  outputDirectory = "build/docs/dokka"
  jdkVersion = 8
  processConfigurations = [ ]
  sourceDirs = [ new File( cloneGSK.outputDirectory.singleFile.absoluteFile, 'src/main/kotlin' ),
                 copyGradleApiSources.outputDirectory.singleFile,
                 file('build-with-core-plugins/buildSrc/build/generated-src/gradle-script-kotlin')]
}
